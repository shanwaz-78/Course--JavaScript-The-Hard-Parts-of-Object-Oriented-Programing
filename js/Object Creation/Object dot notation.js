// Dot Notation(.) we can access object properties or methods using dot notation.

const obj = {};
obj.name = 'Phill'; // That's called we dynamically declare a name property to the obj.
obj.score = 1;
obj.increment = function () {
  obj.increment++
};
console.log(obj);